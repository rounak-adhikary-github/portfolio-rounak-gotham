# 2026-09-25 — portfolioDarkKnight

## Added Case 04 · Off the Record (personal projects)

Rounak asked for a section listing 15 personal projects on GitHub Pages, with a live link per
project and **no "view code" button**.

**What changed**

- `index.html`
  - New `<section id="projects">` between Case 03 (The Record) and Training, preceded by an
    `Off the clock` divider. 15 `<article class="panel card project" data-cat="…">` cards.
  - Each card: `file-no` strip (`SIDE CASE 01 · CITY TOOL`), `<h4>` name, one-line description,
    and exactly one `<a class="btn tiny signal">Open live ↗</a>` with an `.sr-only` suffix so the
    accessible name is unique per card.
  - New nav tab `#projects` → "Side Cases" (6 tabs now).
  - Renumbered: Training → Case 05 (`FILE 05-A/B`), Secure Line → Case 06 (`FILE 06-A/B`).
  - Skills `.filters` bar gained `data-target="#skillGrid"`.
- `css/dark-knight.css` — new subsection **13b. side cases** (`.lead`, `.project-grid`,
  `.project`, `.project .project-foot`, `.skill.is-hidden,.project.is-hidden`). Mobile `.tabs`
  switched to `flex-wrap:wrap` for the 6th tab. Print rules turn each card's button into a plain
  link that prints its `href`.
- `js/dark-knight.js` — the skill-filter block is now generic: it drives every
  `.filters[data-target]` bar against `[data-cat]` cards in the target grid, and adds `.in` to
  newly shown cards (a card hidden at load never reaches the reveal observer).
- `README.md` — layout note, Case 04 row, the fifteen-projects table, and two editing rows.

**Card copy source.** Taken from each site's own `<title>` / meta description (fetched live), not
invented. All 15 returned HTTP 200 on 2026-09-25. Repo slugs do not match display names:
`brainstorm` = Thailand Trip Planner, `rounak-soumili-wedding-album` = শুভ বিবাহ wedding album.

**Categories** (must stay in sync with the chip `data-filter` values): `city` 4, `business` 2,
`skins` 6, `personal` 3.

**Verified** via headless Edge + CDP: 15 cards / 15 links, exact match to Rounak's list, no
duplicates, categories match chips, anchor lands the heading 125px clear of the sticky masthead,
mobile 390px = single column with no tab overflow, "Portfolio Skins" filter shows 6, skills
filter still works (regression check), no console errors.
